# Used by scripts/seed-data.mjs --remote (R2 S3-compatible API).
# Create an R2 API token in the Cloudflare dashboard with write access to the bucket.
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
