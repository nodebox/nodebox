# NodeBox macOS signing and notarization configuration
# Copy this file to .env and fill in your values

# Developer ID Application certificate name
# Find yours with: security find-identity -v -p codesigning
DEVELOPER_ID_APPLICATION="Developer ID Application: Your Name (TEAMID)"

# Keychain profile for notarization (created with xcrun notarytool store-credentials)
KEYCHAIN_PROFILE="nodebox-notarize"

# One-time setup for notarization credentials:
# xcrun notarytool store-credentials "nodebox-notarize" \
#     --apple-id "your@email.com" \
#     --team-id "TEAMID" \
#     --password "app-specific-password"
